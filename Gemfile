source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in hydroponic_mongo.gemspec
gemspec

gem 'pry'

gem 'simplecov', :require => false
